/**
 * Restaurant List App - React Native Entry Point
 * Modern, animated restaurant listing with eye-catching UI
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => App);
